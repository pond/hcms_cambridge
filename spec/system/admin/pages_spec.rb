require "spec_helper.rb"

RSpec.describe "Admin - pages" do
  context "creation" do
    it "can create in a draft state" do
    end

    it "can create in a published state" do
    end

    context "dynamic form behaviour", js: true do
      it "Redactor text entry works" do
      end

      it "changes in page type cause sections to show and hide" do
      end
    end
  end

  context "editing" do
    it "can create a new unpublished revision" do
    end

    it "can create a new published revision" do
    end
  end
end
