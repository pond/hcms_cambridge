PROBLEMATIC ENCOUNTER ORDER ALERT
=================================

An order is in an unexpected state. Usually, this means payment *has* been
made, but an unrecoverable error was then encountered when trying to update
the order's state inside the web site.

Please reconcile payment records against order entries in the admin interface:

* <%= @admin_url %>

<%= render partial: 'encounter_order', object: @encounter_order %>
