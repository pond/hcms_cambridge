<%# Needed since 'show' uses the public site layout for encounters, not the admin layout %>
<%= javascript_include_tag 'minimal' %>

<%= render partial: 'shared/encounter', locals: { encounter: @encounter } %>
