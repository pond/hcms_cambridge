<%
  # Render with:
  #
  #   render partial: 'actions', locals: {
  #     article: Article model instance
  #   }
%>
<td class="admin_actions">
  <%= link_to 'Show', admin_page_article_path(article.page.slug, article.slug) %>
  /
  <%= link_to 'Edit', edit_admin_page_article_path(article.page, article) %>
</td>
<td class="admin_actions for_delete">
  <%= link_to 'Delete', admin_page_article_path(article.page, article), method: :delete, data: { confirm: apphelp_destroy_confirm(article) } %>
</td>
