<h1>Edit page &ldquo;<%= @page.title %>&rdquo;</h1>

<%= render 'form' %>

<% content_for :extra_footer_links do %>
  <div class="extra_footer_links">
    <%= link_to 'Discard changes and show published version', admin_page_path(@page.slug) %> |
    <%= link_to 'Back', :back %>
  </div>
<% end %>
