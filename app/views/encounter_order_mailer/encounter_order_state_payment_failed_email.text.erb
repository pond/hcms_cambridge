<%= @encounter_order.encounter.title.upcase %>
<%= '=' * @encounter_order.encounter.title.size %>

<%
  str = <<~TEXT.squish()
    Unfortunately, there was a problem with your payment of
    #{apphelp_money(@encounter_order.amount_owed, currency: @encounter_order.encounter.currency)}
    for your booking so it is not yet confirmed. To work out what went wrong and
    how to resolve it, please get in touch:
  TEXT
%><%= word_wrap(str, line_width: 78) %>

<%= render partial: 'contact' %>
The booking details are:

<%= render partial: 'encounter_order', object: @encounter_order %>
<%= render partial: 'signature' %>
