<%= @order.event.title.upcase %>
<%= '=' * @order.event.title.size %>

<%= word_wrap("Thank you for your earlier reservation for \"#{@order.event.title}\".", line_width: 78) %>

If you still wish to attend, please lock in the booking by using the link below. The
same link can also be used to cancel the reservation if you've changed your mind.

  <%= ordershelp_magic_link(@order) %>

If you have are any questions, please get in touch:

<%= render partial: 'contact' %>
<%= render partial: 'signature' %>
